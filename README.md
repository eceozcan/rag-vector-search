# Playable RAG Vector Search (Monorepo)

This repository is a TypeScript monorepo that implements a small RAG/vector search demo.

Overview
- Frontend: Next.js + Tailwind UI for Chat and Dashboard
- Backend: Fastify server exposing search, ingestion, and an MCP endpoint
- Shared: TypeScript types

See `docs/` for full instructions (TODO).
