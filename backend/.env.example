# Backend configuration
PORT=4000
EMBEDDING_API_URL=http://127.0.0.1:8000/embed
# Optional: set GEMINI_API_KEY if you want to use a Gemini embedding provider instead
# GEMINI_API_KEY=
# EMBEDDING_PROVIDER=auto
# Optional: protect dashboard and admin endpoints with a shared secret.
JWT_SECRET=super-secret-change-me
